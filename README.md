# PokerBotRe

## Dependencies

Install the required packages:

```powershell
pip install PokerNow undetected-chromedriver setuptools
```
`setuptools` is needed for Python 3.12+ because `undetected-chromedriver` still imports `distutils`.
